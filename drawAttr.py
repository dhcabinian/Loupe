class drawAttr(object):
    CORE_SIZE = 150
    LINK_LENGTH = 50
    LINK_WIDTH = 10
    CORE_SIZE_EXP = 300

    def __init__(self, core_size, link_length, link_width, exp_core_size):
        drawAttr.CORE_SIZE = core_size
        drawAttr.LINK_LENGTH = link_length
        drawAttr.LINK_WIDTH = link_width
        drawAttr.CORE_SIZE_EXP = exp_core_size
